# D8 Sequence 28 recovery repair ledger

Sequence 28 requalifies the supervised delivery boundary from the historical
Codex CLI 0.146 profile to the exact installed Codex Desktop 0.149 production
identity. No app-server, alias resolution, target operation, challenge,
checkpoint, inference, Event 05/06, or unrelated-project access occurred during
requalification.

## Requalification change

- Pin the executable SHA-256, CLI version, code-signing authority, Team ID, and
  application version/build.
- Pin all fourteen raw schemas generated by that executable.
- Require the current thread projection: nullable `projectId`, optional
  `section` and `sectionEnteredAt`, and no removed `isPinned`.
- Update the deterministic fake peer and add rejection tests for old-version,
  executable, signer, team, application, schema, and consumed-field drift.
- Require production identity validation before displaying a confirmation
  challenge.

## Repair loop 1

Finding: semantic-diff digests were initially transcribed as raw-file digests.
The exact-map verifier rejected all fourteen values before tests ran.

Repair: replace them with the SHA-256 values computed from the newly generated
raw 0.149 schema files. The exact-map check then passed 14/14.

## Repair loop 2

Finding: the pre-existing executable authority rejected the canonical macOS
`/Applications` directory because it is root-owned and admin-group-writable.
The installed app bundle and executable were otherwise non-group-writable.

Repair: admit only the exact root-owned `/Applications` directory when it is
not world-writable. Retain path, content-hash, signer, Team ID, application
version/build, and executable-stat validation.

## Deterministic gate corrections

Ruff requested two line-layout changes and identified one unused test import.
Those mechanical corrections did not change the security contract.

## Repair loop 3

Finding: moving identity verification before confirmation caused the
pre-existing non-TTY rejection test to reach a version-probe child first.

Repair: separate TTY admissibility from challenge generation. The public entry
point now rejects non-TTY streams without a child process, verifies the pinned
production identity, and only then permits fresh challenge rendering.

## Review repair loop 4

Claude rejected the first exact-tree candidate with one blocker:
schema-defined null `section` and null `section.appearance` were
over-rejected. It also required reproducible schema evidence, bounded identity
probe accounting, host-independent unit tests, and current optional turn-shape
coverage.

Repair:

- accept and exercise both nullable section forms;
- centralize the exact current turn projection and use it for responses and
  notifications;
- recognize the exact error-notification envelope and fail closed;
- run only two fixed identity probes in owned process groups with bounded
  output, wait, kill, and reap behavior;
- remove installed-app dependencies from unit falsifiers;
- add a committed raw-hash and semantic-path verifier plus sanitized evidence;
  and
- retain the installed-app identity check as an explicit host qualification
  outside portable unit tests.

## Review repair loop 5

Grok rejected the repaired candidate because an identity-probe group-signal
`OSError` raised before direct-child reap, and because the newly recognized
error-notification envelope lacked dedicated falsifiers.

Repair:

- on group-signal failure, kill and reap the direct child before reporting the
  unresolved group-containment failure;
- test the exact fallback ordering;
- centralize and test the exact error-notification envelope;
- prove both `willRetry` values still fail without a retry;
- reject missing, extra, renamed, widened, and wrong-identity error fields; and
- reject unhashable turn status/items-view type drift categorically.

## Qualification

- Raw schemas: 14/14 exact.
- Focused delivery/security tests: 97 passed before the final ordering
  regression.
- Official format, lint, and strict typing gates: passed.
- Complete review-repair exact-worktree tests: 568 passed.
- Focused fake-peer, identity-probe, error-notification, and Darwin cleanup run:
  30 passed.
- Owned Codex app-server descendants after no-live qualification: zero.
- Sanitized candidate privacy scan: passed.
- Unrelated-project access: none.

Final exact-tree gate and independent-review counts are recorded by the
Sequence 28 publication evidence, not predeclared here.
