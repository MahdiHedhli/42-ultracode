# D8 Sequence 28 Codex 0.149 Requalification

The supervised-delivery transport is pinned to the installed Codex Desktop
production identity:

- CLI: `codex-cli 0.149.0-alpha.4.3`
- Application version/build: `26.818.61809` / `7019`
- Team identifier: `2DC432GLL2`
- Executable SHA-256:
  `dd304ffe232fa9e782ed3e5358776d270e394c2fb85cab846f989823f0843313`

Before app-server launch, the transport verifies the sealed executable, exact
CLI version, code-signing authority and team identifier, and application
version/build. Any mismatch fails closed.

The executable path admits macOS's canonical root-owned `/Applications`
directory with its standard admin-group write bit, but never a world-writable
parent. The app bundle and executable remain non-group-writable and are pinned
by signature and content hash.

## Schema profile

The profile pins all fourteen D8 schema files generated by the installed
0.149 executable. Relative to the historical 0.146 bundle, four files are
semantically unchanged and ten are changed. The transport consumes the current
thread shape: `projectId` is required and nullable, `section` and
`sectionEnteredAt` are optional, and the removed `isPinned` field is
rejected.

Schema hashes are compared as an exact mapping. Missing, extra, or changed
entries fail closed.

## Scope

This qualification covers the supervised delivery transport only. It does not
authorize Event 06, checkpoint access, model inference, D4/D5 work, or changes
to local privacy configuration.
